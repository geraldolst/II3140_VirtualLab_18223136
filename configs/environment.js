// Environment bootstrap for client-side config
// TODO: Replace the placeholders with your actual Supabase project URL and anon key.
// You can also load these dynamically (e.g., injected by hosting platform).

window.__SUPABASE_URL = window.__SUPABASE_URL || '';
window.__SUPABASE_ANON_KEY = window.__SUPABASE_ANON_KEY || '';

